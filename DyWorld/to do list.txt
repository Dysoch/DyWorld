--@todo Add pasture farms
--@body 5 tiers, tiny to huge
--
--@todo Add greenhouse
--@body 5 tiers, tiny to huge
--
--@todo Add tree farming
--
--@todo Add methane from cows
--
--@todo Add wheat farming
--
--@todo Add vehicle equipment
--
--@todo Add letters and symbols as flooring
--
--@todo Add color flooring

-- Check this out soon:

local function copyPrototype(type, name, newName, change_results)
    if not data.raw[type][name] then error("type "..type.." "..name.." doesn't exist") end
    local p = table.deepcopy(data.raw[type][name])
    p.name = newName
    if p.minable and p.minable.result then
        p.minable.result = newName
    end
    if change_results then
        if p.place_result then
            p.place_result = newName
        end
        if p.result then
            p.result = newName
        end
        if p.take_result then
            p.take_result = newName
        end
        if p.placed_as_equipment_result then
            p.placed_as_equipment_result = newName
        end
    end
    return p
end

return copyPrototype

local copyPrototype = require "__FARL__/lib"
local farl = copyPrototype("locomotive", "locomotive", "farl")
farl.icon = "__FARL__/graphics/icons/farl.png"
farl.icon_size = 32
farl.max_speed = 0.8
farl.burner.fuel_inventory_size = 4

farl.color = {r = 1, g = 0.80, b = 0, a = 0.8}
--farl.color = {r = 0.8, g = 0.40, b = 0, a = 0.8}
data:extend({farl})
